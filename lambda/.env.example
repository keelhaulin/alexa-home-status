HA_BASE_URL=https://<yourid>.ui.nabu.casa
HA_TOKEN=<your-long-lived-token>
HA_SENSOR=sensor.house_status_summary

# Optional: JSON object mapping area names to entity IDs.
# Paste as a single line, e.g.:
# {"kitchen":{"lights":["light.kitchen_ceiling"],"doors":["binary_sensor.kitchen_back_door"]}}
AREA_MAP_JSON=
